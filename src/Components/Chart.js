import React  from 'react';

    const  Charts =(props) =>{
   
            return(
                <div>
                    <button>{props.name}</button>
                    <button>{props.price}</button>                            
                </div>
                
            )      
        }
        
export default Charts;

